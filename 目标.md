# StoryForge3 目标

> 定位：中文网文全流程创作引擎——从空白页到可发布章节。

## 一句话目标

一个完整的中文网文创作系统，作者从构思开始，完成：

```
创建书籍 → 世界构建 → 角色设计 → 卷纲规划 → 章节生产（计划→起草→审计→修订→确认→导出→状态更新）→ 持续连载
```

## 核心原则

1. **完整创作流程**：从"新建一本书"开始，不依赖已有章节或手动准备的材料。
2. **CCSwitch 双层路由**：全局切换（所有工具跟随）+ 按任务路由（writer/auditor/truth_extractor 各可用不同模型），所有 LLM 调用经 CCSwitch 代理。
3. **SF2 经验继承**：36 机械规则、Truth 管理、5 修订模式、StyleContract、Hook 诊断等已验证能力直接迁移，不重造。
4. **服务层优先**：所有能力通过清晰的 Service Interface 暴露。CLI/Web/Desktop 只是 interface 的消费者，业务逻辑不依赖任何特定前端。
5. **人工确认优先**：approve、export、发布前状态推进必须有清晰的人类确认点。
6. **可审计可回滚**：每次生成、审计、修订、导出都必须留下可追溯记录。

## 产品能力

### 核心管线（单章，继承自 SF2）

```
plan → compose → draft → settle → audit → revise → approve → export → state_update
```

### 已验证能力（从 SF2 迁移）

| 能力 | 说明 |
|------|------|
| 36 机械规则 | 10 AI Tell + 10 Style + 7 Structure + 7 Meta + 2 Report |
| 5 修订模式 | polish / spot_fix / anti_detect / surgical / rework |
| Truth 管理 | snapshot / delta / reconcile / commit + 压缩 + 漂移监控 |
| Prompt Registry | 版本化模板管理 |
| Style Engine | StyleContract → StyleCompiler → StyleGuard |
| Hook/Payoff 诊断 | 密度 + 逾期 + 压力趋势 |
| Cost & Context Budget | Token 估算 + 成本累计 + 优先级分配 |
| 番茄小说导出 | TXT 格式，12 章全部通过校验 |
| 13 阶段状态机 | 完整章节生命周期管理 |

### SF3 新增能力（SF2 未覆盖）

| 能力 | 说明 |
|------|------|
| 书籍创建 | 书名、类型、平台、目标章数、字数配置 |
| 世界构建 | 世界观、力量体系、核心冲突、基本规则 |
| 角色系统 | 主要/次要角色档案、关系矩阵、角色弧线 |
| 卷纲规划 | 卷结构、节奏曲线、关键场景标记 |
| 章节计划 | 意图生成、上下文编译、规则栈组装 |
| CCSwitch 双层路由 | 全局切换 + 按任务模型路由 |
| 服务层 API | 所有业务逻辑通过 Service Interface 暴露 |

## CCSwitch 集成架构

### 双层路由

**第一层：全局切换**
- CCSwitch 切换 provider → 所有工具（ClaudeCode / Codex / SF3）同时跟随
- 工具不碰 provider 配置，不存 API key

**第二层：按任务路由**
- SF3 内部不同任务可指定不同模型：
  - `writer` → 高创意模型（temperature ~0.7）
  - `auditor` → 高精度模型（temperature ~0.3）
  - `truth_extractor` → 低温度结构化输出（temperature ~0.2）
  - `architect` → 高推理模型（世界构建/卷纲规划）
- 模型 ID 通过 SF3 配置指定，请求经 CCSwitch 路由到实际 provider

### 工具集成关系

```
ClaudeCode  ──┐
              ├──→ CCSwitch ──→ Provider A/B/C
Codex        ──┤
              │
SF3 ──→ Service API ──┘
              │
未来前端 ──→ HTTP 适配器 ──→ SF3 Service API ──┘
```

## 服务层设计

### Service Interface 边界

所有业务能力封装为 async Service，前端只通过 Service API 交互：

```
BookService       创建/管理书籍
WorldService      世界构建/规则管理
CharacterService  角色创建/关系管理
VolumeService     卷纲规划/章节分配
ChapterService    章节全生命周期（计划→导出）
AuditService      质量审计（36 规则 + LLM 审计）
TruthService      Truth 提取/存储/查询/压缩
ExportService     平台导出（番茄 TXT / Markdown）
LLMService        CCSwitch 路由（全局 + 按任务）
PromptService     Prompt 模板管理
StyleService      风格契约管理
```

未来前端实现时，只需加一层 HTTP/WebSocket 适配器，Service 层无需改动。

## 暂不做

- 不做前端 UI（先定义好接口，后续实现）
- 不做通用写作 IDE
- 不做英语小说支持（先专注中文）
- 不做同人小说/短篇管线（后续扩展）
- 不做 daemon 自动化模式（后续扩展）
- 不做 EPUB 导出（先番茄 TXT，后续扩展）
- 不复制 StoryForge2 的全部工件系统（迁移验证过的核心，不迁移历史包袱）

## 第一验收目标

用《我是路人甲》完成从零创建到第 3 章的完整验证：

1. **创建书籍**：书名、类型（都市玄幻）、平台（番茄小说）、目标字数
2. **世界构建**：存在感系统、检测中心、异常等级体系
3. **角色设计**：林默（主角）+ 3 个核心配角 + 关系
4. **卷纲规划**：第一卷 10 章结构 + 关键场景
5. **章节生产**：前 3 章通过完整管线（plan → draft → audit → approve → export）
6. **CCSwitch**：全局 + 按任务路由正常工作
7. **Truth**：无 gap，3 章状态连续
8. **导出**：番茄小说 TXT 格式正确
